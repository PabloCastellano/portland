#!/bin/bash

. "$TEST_CODE_DIR/include/testcontrol.sh"

#set -x

generic_tests=`ls $TEST_CODE_DIR/generic/t.*`
for t in $generic_tests; do 
	. "$t"
	repeat_test "$GENERIC_TEST" 1 "XDGUTIL" "xdg-mime"
done

